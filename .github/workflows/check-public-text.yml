name: Check public text

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-public-text:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check local absolute paths
        run: python scripts/check-public-text.py
